<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="AnimalHealth"
        android:roundIcon="@drawable/animalhealth"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.AnimalHealth"
        tools:targetApi="31">
        <activity
            android:name=".ClinicList"
            android:exported="false"
            />
        <activity
            android:name=".EditClinic"
            android:exported="false" />

        <activity android:name=".Log"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ForgotPass"
            android:exported="false" />
        <activity
            android:name=".Register"
            android:exported="false" />
        <activity
            android:name=".Settings"
            android:exported="false" />
        <activity
            android:name=".Clinic"
            android:exported="false" />
        <activity
            android:name=".CreateClinic"
            android:exported="false" />
    </application>

</manifest>